# Defined via `source`
function brikl-install-gh-cli --wraps=env\ TERM=xterm-256color\ ssh\ -t\ coder.poom\ \'bash\ -c\ \"curl\ -fsSL\ https://cli.github.com/packages/githubcli-archive-keyring.gpg\ \|\ sudo\ gpg\ --dearmor\ -o\ /usr/share/keyrings/githubcli-archive-keyring.gpg\"\ \&\&\ bash\ -c\ \"echo\ \\\"deb\ \[arch=\$\(dpkg\ --print-architecture\)\ signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg\]\ https://cli.github.com/packages\ stable\ main\\\"\ \|\ sudo\ tee\ /etc/apt/sources.list.d/github-cli.list\ \>\ /dev/null\"\ \&\&\ sudo\ apt\ update\ \&\&\ sudo\ apt\ install\ gh\' --description alias\ brikl-install-gh-cli\ env\ TERM=xterm-256color\ ssh\ -t\ coder.poom\ \'bash\ -c\ \"curl\ -fsSL\ https://cli.github.com/packages/githubcli-archive-keyring.gpg\ \|\ sudo\ gpg\ --dearmor\ -o\ /usr/share/keyrings/githubcli-archive-keyring.gpg\"\ \&\&\ bash\ -c\ \"echo\ \\\"deb\ \[arch=\$\(dpkg\ --print-architecture\)\ signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg\]\ https://cli.github.com/packages\ stable\ main\\\"\ \|\ sudo\ tee\ /etc/apt/sources.list.d/github-cli.list\ \>\ /dev/null\"\ \&\&\ sudo\ apt\ update\ \&\&\ sudo\ apt\ install\ gh\'
  env TERM=xterm-256color ssh -t coder.poom 'bash -c "curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo gpg --dearmor -o /usr/share/keyrings/githubcli-archive-keyring.gpg" && bash -c "echo \"deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main\" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null" && sudo apt update && sudo apt install gh' $argv; 
end
